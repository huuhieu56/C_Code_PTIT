#include<stdio.h>
#include<math.h>

int main(){
    int t;
    scanf("%d", &t);
    for (int _=1; _<=t; _++){
        int m, n;
        scanf("%d %d", &m, &n);
        int arr[m][n];
        for (int i=0; i<m; i++){
            for (int j=0; j<n; j++){
                scanf("%d", &arr[i][j]);
            }
        }
        printf("Test %d:\n", _);
        for (int i=1; i<m; i++){
            for (int j=1; j<n; j++){
                printf("%d ", arr[i][j]);
            }
            printf("\n");
        }
    }
    return 0;
}