#include<stdio.h>
#include<math.h>

void printFromEnd(int arr[], int n){
    for (int i=n-1; i>=0 ; i--){
        printf("%d ", arr[i]);
    }
    printf("\n");
    return;
}

int main(){
    
    int n;
    scanf("%d", &n);
    int arr[n];
    for (int i=0; i<n; i++){
        scanf("%d", &arr[i]);
    }
    printFromEnd(arr, n);

    return 0;
}